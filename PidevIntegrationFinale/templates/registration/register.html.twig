{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block corp %}
    <h1>Inscription</h1>

    {{ form_start(registrationForm) }}

        {{ form_row(registrationForm.nom, {label: 'Nom Complet'}) }}
        {{ form_row(registrationForm.login, {label: 'Login'}) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Mot de passe'
        }) }}
        {{ form_row(registrationForm.agreeTerms, {label : 'Acceptez nos termes.'})}}

        <button type="submit" class="btn" href="{{ path('app_login') }}">Confirmer</button>

    {{ form_end(registrationForm) }}
{% endblock %}
